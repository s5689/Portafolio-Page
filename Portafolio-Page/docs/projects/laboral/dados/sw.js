var __wpo = {
  assets: {
    main: [
      '/static/media/sfx_shieldUp.bf74bd37.ogg',
      '/28230c787600c889f06b8fd0a472b7dd.webp',
      '/static/media/surface_wood_tray1.b4098b69.mp3',
      '/static/media/dicehit_wood12.d1979f92.mp3',
      '/static/media/dicehit_plastic3.db808cd5.mp3',
      '/static/media/surface_metal8.5d8b08a8.mp3',
      '/static/media/dicehit_wood8.b9338401.mp3',
      '/static/media/dicehit_metal8.077ad82c.mp3',
      '/static/media/surface_metal5.6a54a661.mp3',
      '/static/media/dicehit_metal9.994d84a5.mp3',
      '/static/media/dicehit_wood7.dc77ec2c.mp3',
      '/73288b732382667b107627e09650004c.webp',
      '/b87b9ba532ace76ae9f6edfe9f72ded2.ttf',
      '/f90298690b11ee585e98ba549958386c.webp',
      '/static/media/dicehit_wood2.26239d5e.mp3',
      '/static/media/dicehit_coin4.8ad82113.mp3',
      '/static/media/surface_felt3.463962e0.mp3',
      '/static/media/dicehit_coin6.41350c78.mp3',
      '/0ab54153eeeca0ce03978cc463b257f7.woff2',
      '/8e3c7f5520f5ae906c6cf6d7f3ddcd19.eot',
      '/static/media/dicehit_coin1.8296daf3.mp3',
      '/static/media/dicehit_metal12.29540dc1.mp3',
      '/static/media/dicehit_metal6.10be273a.mp3',
      '/static/media/dicehit_metal1.56bac0b7.mp3',
      '/static/media/dicehit_wood9.be50c59f.mp3',
      '/faff92145777a3cbaf8e7367b4807987.woff',
      '/static/media/surface_felt5.3c7283c9.mp3',
      '/static/media/surface_wood_tray7.c90eaefb.mp3',
      '/1281572112c1ae75c1053e1fff6382be.webp',
      '/static/media/surface_wood_table1.ccd8a675.mp3',
      '/701ae6abd4719e9c2ada3535a497b341.eot',
      '/c5ebe0b32dc1b5cc449a76c4204d13bb.ttf',
      '/13db00b7a34fee4d819ab7f9838cc428.eot',
      '/634ef539dfe447f5141a5b3f140ceb37.webp',
      '/b43fa84c782166b7531106401844edda.webp',
      '/static/media/surface_wood_table4.108b5444.mp3',
      '/b672a18e3b36a7ba9164430a4ce2981a.webp',
      '/647583672b7548c58d2b35fecf94c172.webp',
      '/ddfdf132d63d4d91e9e35db7c9573cc7.webp',
      '/static/media/surface_metal2.6a315e1b.mp3',
      '/favicon2.png',
      '/static/media/surface_wood_tray2.8ba17cd0.mp3',
      '/static/media/surface_metal6.db414448.mp3',
      '/static/media/surface_felt2.ec12945c.mp3',
      '/static/media/dicehit_plastic8.0f62369c.mp3',
      '/static/media/dicehit_wood3.8a901681.mp3',
      '/static/media/dicehit_plastic6.237db2e7.mp3',
      '/static/media/dicehit_metal10.52ad4277.mp3',
      '/4e9e12fda37b08caf094f4323b9905c1.webp',
      '/static/media/surface_metal1.02a471a5.mp3',
      '/cd6c777f1945164224dee082abaea03a.woff2',
      '/9789a55f50b68f350f7c5a705ee589ba.webp',
      '/a72678ada6f14eaf74237d5747c0a653.webp',
      '/static/media/surface_felt6.e6cdc90f.mp3',
      '/static/media/dicehit_plastic4.ffc38233.mp3',
      '/a1a749e89f578a49306ec2b055c073da.svg',
      '/static/media/dicehit_wood1.33481740.mp3',
      '/static/media/dicehit_wood6.243f4a67.mp3',
      '/static/media/dicehit_plastic1.d36e13ea.mp3',
      '/static/media/dicehit_coin5.9e80d572.mp3',
      '/static/media/surface_metal3.fe632990.mp3',
      '/04f3bee52674bb02e045d68aeb5a9bbd.webp',
      '/static/media/dicehit_plastic12.3a669954.mp3',
      '/d608aba40c43c7d308d1a8accc2990b8.webp',
      '/static/media/surface_wood_table3.35d5d553.mp3',
      '/static/media/surface_felt4.507d6c56.mp3',
      '/static/media/dicehit_plastic10.d39127c3.mp3',
      '/static/media/dicehit_plastic9.e80279c7.mp3',
      '/355dd78b593f383ba7bb692903859411.jpg',
      '/static/media/surface_wood_table5.cfc58e63.mp3',
      '/static/media/surface_metal9.993666bc.mp3',
      '/static/media/surface_metal7.ae63a34f.mp3',
      '/471bde11a2ac5c548cc61f4ccdcbd0a2.webp',
      '/static/media/surface_wood_tray5.b1ddd973.mp3',
      '/static/media/dicehit_wood5.41c39c9b.mp3',
      '/1418d426f6b5eca07365d2ebc2cfc6e3.webp',
      '/static/media/dicehit_plastic5.28d35ede.mp3',
      '/a9245559a8486a284a0c6538e584ed8e.webp',
      '/static/media/sfx_lose.a8ac6045.ogg',
      '/static/media/dicehit_metal3.f51c9ac6.mp3',
      '/static/media/surface_metal4.e027701c.mp3',
      '/8cd2a5d9016317c8484af019e35ee5cd.webp',
      '/9c74e172f87984c48ddf5c8108cabe67.png',
      '/f76aeb7e52154613c3dc7f5162f4953c.webp',
      '/static/media/dicehit_plastic7.a3db4f0e.mp3',
      '/static/media/dicehit_wood11.0544758b.mp3',
      '/static/media/dicehit_coin2.0c7ce586.mp3',
      '/static/media/surface_wood_table6.ca6863a8.mp3',
      '/78d591cb5a130ebdb2cbb41eeca51f58.webp',
      '/static/media/surface_felt7.df8a2544.mp3',
      '/static/media/surface_wood_tray6.861e1c9a.mp3',
      '/c856fe7c78ee63f8603e0d57e91e5f7f.webp',
      '/ad97afd3337e8cda302d10ff5a4026b8.ttf',
      '/static/media/surface_wood_table2.8b07fc1f.mp3',
      '/b5706c4e74296e5cdc3808ddf0aee2f4.webp',
      '/static/media/dicehit_coin3.f312e775.mp3',
      '/ef60a4f6c25ef7f39f2d25a748dbecfe.woff',
      '/static/media/dicehit_plastic11.dee08a9b.mp3',
      '/a046592bac8f2fd96e994733faf3858c.woff',
      '/static/media/dicehit_metal7.6b629d38.mp3',
      '/static/media/dicehit_plastic14.9e69e159.mp3',
      '/static/media/dicehit_metal5.4d566628.mp3',
      '/77e32d2979c15c835e2f602c19e3a43e.webp',
      '/e8c322de9658cbeb8a774b6624167c2c.woff2',
      '/b8961a089f11516f5ee2c059052aa22e.webp',
      '/static/media/dicehit_wood4.653b4f31.mp3',
      '/static/media/surface_wood_table7.90215ad3.mp3',
      '/static/media/dicehit_metal11.3a7b2ae0.mp3',
      '/82f60bd0b94a1ed68b1e6e309ce2e8c3.svg',
      '/static/media/surface_wood_tray4.4436c5f6.mp3',
      '/static/media/surface_wood_tray3.2699cf88.mp3',
      '/static/media/dicehit_plastic15.6883cca5.mp3',
      '/static/media/dicehit_wood10.0aed06f9.mp3',
      '/static/media/dicehit_metal4.6b2f6c70.mp3',
      '/962a1bf31c081691065fe333d9fa8105.svg',
      '/static/media/surface_felt1.9ff7181c.mp3',
      '/static/media/dicehit_plastic13.8f495321.mp3',
      '/static/media/dicehit_plastic2.0b5cf5c8.mp3',
      '/static/media/dicehit_metal2.ca2d3847.mp3',
      '/648c30bd9c1ffd1ab9bf0d4efcb5dc6e.webp',
      '/runtime.2026696cc05bc277101f.js',
      '/',
    ],
    additional: [
      '/npm.intl.bd8f19d9c7d9ab22c1c5.chunk.js',
      '/main.b9d15f9887f52be57f90.chunk.js',
      '/npm.babel.4c1938caea6c5100ec72.chunk.js',
      '/npm.core-js.a82f6c631796108b7cb1.chunk.js',
      '/npm.intl-messageformat.d458ce0e0ac21343e4d0.chunk.js',
      '/npm.intl-relativeformat.1bab2cf3f5aed1fbd441.chunk.js',
      '/npm.lodash-es.f3f0522f3194f351d4fb.chunk.js',
      '/npm.react-app-polyfill.c823b5fdbbb49517598b.chunk.js',
      '/npm.react-redux.b206126775c9c194b87e.chunk.js',
      '/npm.semantic-ui-css.4871700cd6aaba58b160.chunk.js',
      '/npm.semantic-ui-react.3680a044bc198b7de2be.chunk.js',
      '/12.e85530c1149c86b5e89e.chunk.js',
    ],
    optional: [],
  },
  externals: [],
  hashesMap: {
    b66c5c6a58d14d6369f964287bcd6f81e1172519: '/static/media/sfx_shieldUp.bf74bd37.ogg',
    '0e4087e3ad5f62245ceb2cbb92ccc16e0e0ec11f': '/28230c787600c889f06b8fd0a472b7dd.webp',
    '559061f4f3b2604561cacf9cf303c9609206d83a': '/static/media/surface_wood_tray1.b4098b69.mp3',
    '3efb8f1349f13194e877d7e3e361ac1a516a04f0': '/static/media/dicehit_wood12.d1979f92.mp3',
    '8dda8538e64e69be2df259c76c4c8f1c4b74b525': '/static/media/dicehit_plastic3.db808cd5.mp3',
    ca15c440d491379672749f3477ee8b8feb4b08ee: '/static/media/surface_metal8.5d8b08a8.mp3',
    '7001091ca02b4af2c8c2c3af429efb4fbdfb1242': '/static/media/dicehit_wood8.b9338401.mp3',
    e769b41a31f9d9f725220367744ee41c07fe3f56: '/static/media/dicehit_metal8.077ad82c.mp3',
    '19e35811256c78f473f91bacff6e35e0bccd6bdf': '/static/media/surface_metal5.6a54a661.mp3',
    '96977f760b69ea94e2e36af75bd82cbad5c0d5d8': '/static/media/dicehit_metal9.994d84a5.mp3',
    '8196e9f12cfe37c474383da9a3e031814cc7744f': '/static/media/dicehit_wood7.dc77ec2c.mp3',
    '7d65c3153103b8c07f7b453220edf6736fd23d0b': '/73288b732382667b107627e09650004c.webp',
    cedd7227091b22f873e3856d84c3dfd974745048: '/b87b9ba532ace76ae9f6edfe9f72ded2.ttf',
    '575848134810c3d4bd895c236486a507d9e5ff7c': '/f90298690b11ee585e98ba549958386c.webp',
    c79996a1bdc8662157b7af83ff51945f7a83b156: '/static/media/dicehit_wood2.26239d5e.mp3',
    '661382b13e2d97abc42645d217909e397450fee5': '/static/media/dicehit_coin4.8ad82113.mp3',
    '0a94f2304642d0b35ae733959ea25755c03e205f': '/static/media/surface_felt3.463962e0.mp3',
    b2ceb959cf7db065dff194d98e65c6c22289d377: '/static/media/dicehit_coin6.41350c78.mp3',
    '6ec6d36cb2464b4e821cfabb532f310bd342601c': '/0ab54153eeeca0ce03978cc463b257f7.woff2',
    b7de2e1d65766852486de24b36a46240f4ae5994: '/8e3c7f5520f5ae906c6cf6d7f3ddcd19.eot',
    '4fe3de4f0f4544a499fb13d0b17c16e7462bf70f': '/static/media/dicehit_coin1.8296daf3.mp3',
    '16ed58749add3729fb64d29141774822cc5de7b4': '/static/media/dicehit_metal12.29540dc1.mp3',
    '8ca95ebefed24b3b2fd8541a4acbd4097dd1619a': '/static/media/dicehit_metal6.10be273a.mp3',
    '2ab5e3f6fd37ff2f06980ea9918e49f7a2221163': '/static/media/dicehit_metal1.56bac0b7.mp3',
    '266d914ecf4f7d744ea3cb1c8260817d92ff63a5': '/static/media/dicehit_wood9.be50c59f.mp3',
    '9c293328f39dc54bd654d273d0cc5af0d11905c6': '/faff92145777a3cbaf8e7367b4807987.woff',
    '2cc08cae868679082f9603d7ba9d47aebe20619c': '/static/media/surface_felt5.3c7283c9.mp3',
    ce76cb35287cef9c3d71c89e991fc26c1e654e47: '/static/media/surface_wood_tray7.c90eaefb.mp3',
    '18b20b5c5d22b61d9dc578ee8d315cca59249c42': '/1281572112c1ae75c1053e1fff6382be.webp',
    '28c6912611752133f933811d3d33a4a4fde3b89c': '/static/media/surface_wood_table1.ccd8a675.mp3',
    '983434f5c40d4aebc159953a99948a9f1d867353': '/701ae6abd4719e9c2ada3535a497b341.eot',
    '3f8221e9e98b788df278149907648fbe042e7bc1': '/c5ebe0b32dc1b5cc449a76c4204d13bb.ttf',
    '71095026a53b66beb9465bea1e73f910dcae19d5': '/13db00b7a34fee4d819ab7f9838cc428.eot',
    '7fbb0fc3be07eb223fbc1fc596058ec5f6538551': '/634ef539dfe447f5141a5b3f140ceb37.webp',
    df4dc6436237a2b8f233cc83fe4bd713e6a778cf: '/b43fa84c782166b7531106401844edda.webp',
    f901bcba1e99de707cf8219a4127c8693db60967: '/static/media/surface_wood_table4.108b5444.mp3',
    '76ca344d07d1a5b618a7286e470957b9698d88cb': '/b672a18e3b36a7ba9164430a4ce2981a.webp',
    fb5a2dec5a588e75fbf6a4985abe766f9d394145: '/647583672b7548c58d2b35fecf94c172.webp',
    '77483e1f403cd1f9550769ce3b5c7595f0934256': '/ddfdf132d63d4d91e9e35db7c9573cc7.webp',
    '2890f8705f71d1edfe5cf5207cda50062ad3f3f7': '/static/media/surface_metal2.6a315e1b.mp3',
    '591af1dbbfe6785eba67f7e78ecb76a6feca88cc': '/favicon2.png',
    '1844d9e6165b27ae9bfb1ea2750e86929165f390': '/static/media/surface_wood_tray2.8ba17cd0.mp3',
    '55974a4f26a79c8c1bea9f6ff646dfb4629c80fa': '/static/media/surface_metal6.db414448.mp3',
    '0995409236ccf95b3a588381e43cc13e5c40a9e0': '/static/media/surface_felt2.ec12945c.mp3',
    '8a4e6d96b8ce1b20d8502407a538dbf5856b23a0': '/static/media/dicehit_plastic8.0f62369c.mp3',
    af467132edff990b08c0b0bd4e7699c16e63258c: '/static/media/dicehit_wood3.8a901681.mp3',
    '1df4d7bef5370557e20180bcd1de47596f0c76f3': '/static/media/dicehit_plastic6.237db2e7.mp3',
    '85ec72c1696bb0bbd192909387309d0a5c253c8d': '/static/media/dicehit_metal10.52ad4277.mp3',
    '1333c9f48020c9bb87d2e36da271292c77bb22d9': '/4e9e12fda37b08caf094f4323b9905c1.webp',
    dafce68eaa76f304668d97910834bf6ed7baf574: '/static/media/surface_metal1.02a471a5.mp3',
    e5b717fe5d8f54038e432721dd530e7da505447b: '/cd6c777f1945164224dee082abaea03a.woff2',
    '5b3b7c4f583575bdf4ef3ed0b010225ba809525c': '/9789a55f50b68f350f7c5a705ee589ba.webp',
    '6ac17bb596ac6cec949c7fe5925a412061a9cf79': '/a72678ada6f14eaf74237d5747c0a653.webp',
    '92b265ac96111bc53a88847596c88d0acd1d8143': '/static/media/surface_felt6.e6cdc90f.mp3',
    fcaeb6f926ddd106cc9a2dc37d7e35913c884488: '/static/media/dicehit_plastic4.ffc38233.mp3',
    '1984f7fb24242adbb4186f4ff3d27d75cf0a248c': '/a1a749e89f578a49306ec2b055c073da.svg',
    '3394c289c0a598d84f2a32fc6625cfeb91d23fa2': '/static/media/dicehit_wood1.33481740.mp3',
    '125edb16279774eb61c42791fc74286ca5ddf0e6': '/static/media/dicehit_wood6.243f4a67.mp3',
    '86ab960cffcecabd3d5d1afd6b0fe611feceee42': '/static/media/dicehit_plastic1.d36e13ea.mp3',
    '45dd31d292a2516ee3289cc3d6400350290d8b92': '/static/media/dicehit_coin5.9e80d572.mp3',
    a137bea65d2a273fc537b717f9856ee95f3c1968: '/static/media/surface_metal3.fe632990.mp3',
    '4952cd3f8020fa023e8cd8b1f549a3b2821b9bba': '/04f3bee52674bb02e045d68aeb5a9bbd.webp',
    cd6be16021a4ca2a093ab84b44bd114ba9ee73c0: '/static/media/dicehit_plastic12.3a669954.mp3',
    f341eabb07983d6d667cdf84c4bcdce377bad487: '/d608aba40c43c7d308d1a8accc2990b8.webp',
    ea522c882b50dafc5cfcdb28f6c5a953a2634bf4: '/static/media/surface_wood_table3.35d5d553.mp3',
    '305ba468ca28021042669fe4a44293d84b02b207': '/static/media/surface_felt4.507d6c56.mp3',
    '94faeda800792ea2035a242a0d4a5585797ec59f': '/static/media/dicehit_plastic10.d39127c3.mp3',
    d6b1cb727d753cae97a979ecbdaf0a64db90c1d6: '/static/media/dicehit_plastic9.e80279c7.mp3',
    '7dda5bfc3221f5e52a7a1f659b074ef1998498c3': '/355dd78b593f383ba7bb692903859411.jpg',
    '612e7f90ce176b16e30d61ecfa16daeaa6047f76': '/static/media/surface_wood_table5.cfc58e63.mp3',
    cf76833f3489379f5b88d551927ec294a4706d6d: '/static/media/surface_metal9.993666bc.mp3',
    d45e74b1d52676eeb96bbca1acc296257de8c0e2: '/static/media/surface_metal7.ae63a34f.mp3',
    '5138c7ae4e6fd9e711ff729ab51d521e3b6faeae': '/471bde11a2ac5c548cc61f4ccdcbd0a2.webp',
    f835ac99f5599ada8a64e269d10aabd7caded5c5: '/static/media/surface_wood_tray5.b1ddd973.mp3',
    '178583571591020db0002da5aa6964c21914b61a': '/static/media/dicehit_wood5.41c39c9b.mp3',
    '5a7f7430805203b6378f6cd0093a634216eca1c1': '/1418d426f6b5eca07365d2ebc2cfc6e3.webp',
    '0be5294c6095059742b1315d46e463a58360b75f': '/static/media/dicehit_plastic5.28d35ede.mp3',
    '8f2e8b9778d7ae7c31803dcd8218c557d1c75ab7': '/a9245559a8486a284a0c6538e584ed8e.webp',
    '084de2274446b1db45a67fad7bc5428e8a9608f8': '/static/media/sfx_lose.a8ac6045.ogg',
    aa5e7d376f5affe7bcf5f1eb23f23dd66a8cd892: '/static/media/dicehit_metal3.f51c9ac6.mp3',
    c0acb846b112a0b42de520a8c990d31fa201c8c8: '/static/media/surface_metal4.e027701c.mp3',
    bb36795ec2ea987c0cd4fe2416d4ad458d856204: '/8cd2a5d9016317c8484af019e35ee5cd.webp',
    '4a552ecb123ded5040ddda01d626103cc65db055': '/9c74e172f87984c48ddf5c8108cabe67.png',
    '5388ec83f70749050b896216e904e04b4424b0a9': '/f76aeb7e52154613c3dc7f5162f4953c.webp',
    '067005632e011d1b302a4e7326ab1c8a87915edc': '/static/media/dicehit_plastic7.a3db4f0e.mp3',
    '0af56edfb8bef845e82a5fda07148647d717adc2': '/static/media/dicehit_wood11.0544758b.mp3',
    e6d5a242713057e2b00d6719ba43e9efb89a6b94: '/static/media/dicehit_coin2.0c7ce586.mp3',
    '97f45075369303ccf1b18317233fff148e96722a': '/static/media/surface_wood_table6.ca6863a8.mp3',
    ff15b1a77638c91d5ccffb97bde13989e19bb484: '/78d591cb5a130ebdb2cbb41eeca51f58.webp',
    '52d283de892ee786528f4b16cd45247bb942ad30': '/static/media/surface_felt7.df8a2544.mp3',
    '5b19369562acb7296b592c6a68716ea3f4479cfc': '/static/media/surface_wood_tray6.861e1c9a.mp3',
    '3bb8a2e80bfcc5619f627eb364dcc5659f93aac0': '/c856fe7c78ee63f8603e0d57e91e5f7f.webp',
    '7f80f427d3dee7d4ff27a1546b4ad5acd9810a88': '/ad97afd3337e8cda302d10ff5a4026b8.ttf',
    '0a0629ed01432a29802d69d1b4f86cec91431f2e': '/static/media/surface_wood_table2.8b07fc1f.mp3',
    '19eb49d953d9ca1f595837208b872c84f393ea76': '/b5706c4e74296e5cdc3808ddf0aee2f4.webp',
    '96ec22a5c50307a8ba3f8845cdf8c2b50250a1f8': '/static/media/dicehit_coin3.f312e775.mp3',
    '75e53a093de3a9f82a4098a8a1066fc8740bc284': '/ef60a4f6c25ef7f39f2d25a748dbecfe.woff',
    '39ad3851042c06dd2bdc9c61ed52691c0b75bbfd': '/static/media/dicehit_plastic11.dee08a9b.mp3',
    bb17cde5f1519a4e36b89383ff7e778a168f4933: '/a046592bac8f2fd96e994733faf3858c.woff',
    c893c7e63f0911db6564076a8da485a7c302cd96: '/static/media/dicehit_metal7.6b629d38.mp3',
    '65fb73838420677f4ea102f415b93aa33700bfa5': '/static/media/dicehit_plastic14.9e69e159.mp3',
    '62a693e2cc9fb29f428a63df4cc510ddef481c1c': '/static/media/dicehit_metal5.4d566628.mp3',
    c718a9724eea65f2ab4377de207419646c8464e3: '/77e32d2979c15c835e2f602c19e3a43e.webp',
    db06af71da4197a4e1bd553d124725a8081c13f0: '/e8c322de9658cbeb8a774b6624167c2c.woff2',
    c83e6d4b634384850865f89e312bdb8b2dc48b51: '/b8961a089f11516f5ee2c059052aa22e.webp',
    '6fc61741dbc66b0ebbcbcb5b6d7d0f6717a3da27': '/static/media/dicehit_wood4.653b4f31.mp3',
    '38486132b0f785dd0e9a64a4f898e07566b50ae2': '/static/media/surface_wood_table7.90215ad3.mp3',
    a0debd2375045b37fa7c7ffd84c1cf7c5c7a31aa: '/static/media/dicehit_metal11.3a7b2ae0.mp3',
    '3cdd832299463d7ea83743f7ff5b7a937626dcd0': '/82f60bd0b94a1ed68b1e6e309ce2e8c3.svg',
    d855c3b61a51823aeacb03f4276423466bb20d4a: '/static/media/surface_wood_tray4.4436c5f6.mp3',
    '84e7aac7f4d2181d659c5be7601631a1fbdeed5c': '/static/media/surface_wood_tray3.2699cf88.mp3',
    d994e21bb1fa6560c917e1fa6a083cc882cfcc4a: '/static/media/dicehit_plastic15.6883cca5.mp3',
    '1f436ccfb7468e7efb97637f51925e824286a618': '/static/media/dicehit_wood10.0aed06f9.mp3',
    '3264835b889f45149670364ffaf451390b750c46': '/static/media/dicehit_metal4.6b2f6c70.mp3',
    ebcf4cc735912d8da1ab2395738b7950df3df9ff: '/962a1bf31c081691065fe333d9fa8105.svg',
    '844f7ea988b2e09ff174d6a1e7bf90d424263f4b': '/static/media/surface_felt1.9ff7181c.mp3',
    '8fefb87ff71d011620913397c1b328cd5881f247': '/static/media/dicehit_plastic13.8f495321.mp3',
    '7aad4a7292f6c50781ee5293de380d5e797800b9': '/static/media/dicehit_plastic2.0b5cf5c8.mp3',
    c1e77071cb8ef44d35803c1a7aa7479f7f988ad7: '/static/media/dicehit_metal2.ca2d3847.mp3',
    '27ae946887b9b780f48bcc5d2b7a90da9b67bcea': '/648c30bd9c1ffd1ab9bf0d4efcb5dc6e.webp',
    '1e7fd4463861aeb6a378cf7fb56273f34f47d204': '/npm.intl.bd8f19d9c7d9ab22c1c5.chunk.js',
    '88b9186516bb7c83089e443e0bb3d6dfd15774df': '/main.b9d15f9887f52be57f90.chunk.js',
    '1e4c219d708254c872aefc497df325300ec52cc2': '/npm.babel.4c1938caea6c5100ec72.chunk.js',
    '692cd7f1195766b1f8b520b2c7c1da79a6c4f29b': '/npm.core-js.a82f6c631796108b7cb1.chunk.js',
    '7856bb33320ccbe33c2f96d25e546c65e2f861b5': '/npm.intl-messageformat.d458ce0e0ac21343e4d0.chunk.js',
    a37bcdb53d1596e40a0ff7f8d473e038d6cc2995: '/npm.intl-relativeformat.1bab2cf3f5aed1fbd441.chunk.js',
    '39cb86cf9ad1d5f972fd1d29cf27ad075e994575': '/npm.lodash-es.f3f0522f3194f351d4fb.chunk.js',
    '80c2ab2a9d58be97ff120f67cd3ddee45b508f39': '/npm.react-app-polyfill.c823b5fdbbb49517598b.chunk.js',
    fc0c06748cb1f5c3bec7a648aced1b132ab41b69: '/npm.react-redux.b206126775c9c194b87e.chunk.js',
    ac91f698c6eb88f5baaea658f01d1f4c17741086: '/npm.semantic-ui-css.4871700cd6aaba58b160.chunk.js',
    d2e718deefd5bd4b2e1f784f75c547233a3cb848: '/npm.semantic-ui-react.3680a044bc198b7de2be.chunk.js',
    e2bfce6bfc64e0903091092cd1b49c51bc7e3b14: '/runtime.2026696cc05bc277101f.js',
    '1bf213282292ce0a6ef62f4c9d17b385efcd4e56': '/12.e85530c1149c86b5e89e.chunk.js',
    '4102836d645fd93feb18cd760ce1e4ce51b97160': '/',
  },
  strategy: 'changed',
  responseStrategy: 'cache-first',
  version: '2/9/2023 11:23:52 p. m.',
  name: 'webpack-offline',
  pluginVersion: '5.0.6',
  relativePaths: false,
};

/******/ (function (modules) {
  // webpackBootstrap
  /******/ // The module cache
  /******/ var installedModules = {};
  /******/
  /******/ // The require function
  /******/ function __webpack_require__(moduleId) {
    /******/
    /******/ // Check if module is in cache
    /******/ if (installedModules[moduleId]) {
      /******/ return installedModules[moduleId].exports;
      /******/
    }
    /******/ // Create a new module (and put it into the cache)
    /******/ var module = (installedModules[moduleId] = {
      /******/ i: moduleId,
      /******/ l: false,
      /******/ exports: {},
      /******/
    });
    /******/
    /******/ // Execute the module function
    /******/ modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
    /******/
    /******/ // Flag the module as loaded
    /******/ module.l = true;
    /******/
    /******/ // Return the exports of the module
    /******/ return module.exports;
    /******/
  }
  /******/
  /******/
  /******/ // expose the modules object (__webpack_modules__)
  /******/ __webpack_require__.m = modules;
  /******/
  /******/ // expose the module cache
  /******/ __webpack_require__.c = installedModules;
  /******/
  /******/ // define getter function for harmony exports
  /******/ __webpack_require__.d = function (exports, name, getter) {
    /******/ if (!__webpack_require__.o(exports, name)) {
      /******/ Object.defineProperty(exports, name, { enumerable: true, get: getter });
      /******/
    }
    /******/
  };
  /******/
  /******/ // define __esModule on exports
  /******/ __webpack_require__.r = function (exports) {
    /******/ if (typeof Symbol !== 'undefined' && Symbol.toStringTag) {
      /******/ Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
      /******/
    }
    /******/ Object.defineProperty(exports, '__esModule', { value: true });
    /******/
  };
  /******/
  /******/ // create a fake namespace object
  /******/ // mode & 1: value is a module id, require it
  /******/ // mode & 2: merge all properties of value into the ns
  /******/ // mode & 4: return value when already ns object
  /******/ // mode & 8|1: behave like require
  /******/ __webpack_require__.t = function (value, mode) {
    /******/ if (mode & 1) value = __webpack_require__(value);
    /******/ if (mode & 8) return value;
    /******/ if (mode & 4 && typeof value === 'object' && value && value.__esModule) return value;
    /******/ var ns = Object.create(null);
    /******/ __webpack_require__.r(ns);
    /******/ Object.defineProperty(ns, 'default', { enumerable: true, value: value });
    /******/ if (mode & 2 && typeof value != 'string')
      for (var key in value)
        __webpack_require__.d(
          ns,
          key,
          function (key) {
            return value[key];
          }.bind(null, key)
        );
    /******/ return ns;
    /******/
  };
  /******/
  /******/ // getDefaultExport function for compatibility with non-harmony modules
  /******/ __webpack_require__.n = function (module) {
    /******/ var getter =
      module && module.__esModule
        ? /******/ function getDefault() {
            return module['default'];
          }
        : /******/ function getModuleExports() {
            return module;
          };
    /******/ __webpack_require__.d(getter, 'a', getter);
    /******/ return getter;
    /******/
  };
  /******/
  /******/ // Object.prototype.hasOwnProperty.call
  /******/ __webpack_require__.o = function (object, property) {
    return Object.prototype.hasOwnProperty.call(object, property);
  };
  /******/
  /******/ // __webpack_public_path__
  /******/ __webpack_require__.p = '/Portafolio-Page/docs/projects/laboral/dados/';
  /******/
  /******/
  /******/ // Load entry module and return exports
  /******/ return __webpack_require__((__webpack_require__.s = '22249e1ea7baa06e7c1b'));
  /******/
})(
  /************************************************************************/
  /******/ {
    /***/ '22249e1ea7baa06e7c1b': /***/ function (module, exports, __webpack_require__) {
      'use strict';

      (function () {
        var waitUntil = ExtendableEvent.prototype.waitUntil;
        var respondWith = FetchEvent.prototype.respondWith;
        var promisesMap = new WeakMap();

        ExtendableEvent.prototype.waitUntil = function (promise) {
          var extendableEvent = this;
          var promises = promisesMap.get(extendableEvent);

          if (promises) {
            promises.push(Promise.resolve(promise));
            return;
          }

          promises = [Promise.resolve(promise)];
          promisesMap.set(extendableEvent, promises);

          // call original method
          return waitUntil.call(
            extendableEvent,
            Promise.resolve().then(function processPromises() {
              var len = promises.length;

              // wait for all to settle
              return Promise.all(
                promises.map(function (p) {
                  return p['catch'](function () {});
                })
              ).then(function () {
                // have new items been added? If so, wait again
                if (promises.length != len) return processPromises();
                // we're done!
                promisesMap['delete'](extendableEvent);
                // reject if one of the promises rejected
                return Promise.all(promises);
              });
            })
          );
        };

        FetchEvent.prototype.respondWith = function (promise) {
          this.waitUntil(promise);
          return respondWith.call(this, promise);
        };
      })();
      ('use strict');

      if (typeof DEBUG === 'undefined') {
        var DEBUG = false;
      }

      function WebpackServiceWorker(params, helpers) {
        var cacheMaps = helpers.cacheMaps;
        // navigationPreload: true, { map: (URL) => URL, test: (URL) => boolean }
        var navigationPreload = helpers.navigationPreload;

        // (update)strategy: changed, all
        var strategy = params.strategy;
        // responseStrategy: cache-first, network-first
        var responseStrategy = params.responseStrategy;

        var assets = params.assets;

        var hashesMap = params.hashesMap;
        var externals = params.externals;

        var prefetchRequest = params.prefetchRequest || {
          credentials: 'same-origin',
          mode: 'cors',
        };

        var CACHE_PREFIX = params.name;
        var CACHE_TAG = params.version;
        var CACHE_NAME = CACHE_PREFIX + ':' + CACHE_TAG;

        var PRELOAD_CACHE_NAME = CACHE_PREFIX + '$preload';
        var STORED_DATA_KEY = '__offline_webpack__data';

        mapAssets();

        var allAssets = [].concat(assets.main, assets.additional, assets.optional);

        self.addEventListener('install', function (event) {
          console.log('[SW]:', 'Install event');

          var installing = undefined;

          if (strategy === 'changed') {
            installing = cacheChanged('main');
          } else {
            installing = cacheAssets('main');
          }

          event.waitUntil(installing);
        });

        self.addEventListener('activate', function (event) {
          console.log('[SW]:', 'Activate event');

          var activation = cacheAdditional();

          // Delete all assets which name starts with CACHE_PREFIX and
          // is not current cache (CACHE_NAME)
          activation = activation.then(storeCacheData);
          activation = activation.then(deleteObsolete);
          activation = activation.then(function () {
            if (self.clients && self.clients.claim) {
              return self.clients.claim();
            }
          });

          if (navigationPreload && self.registration.navigationPreload) {
            activation = Promise.all([activation, self.registration.navigationPreload.enable()]);
          }

          event.waitUntil(activation);
        });

        function cacheAdditional() {
          if (!assets.additional.length) {
            return Promise.resolve();
          }

          if (DEBUG) {
            console.log('[SW]:', 'Caching additional');
          }

          var operation = undefined;

          if (strategy === 'changed') {
            operation = cacheChanged('additional');
          } else {
            operation = cacheAssets('additional');
          }

          // Ignore fail of `additional` cache section
          return operation['catch'](function (e) {
            console.error('[SW]:', 'Cache section `additional` failed to load');
          });
        }

        function cacheAssets(section) {
          var batch = assets[section];

          return caches
            .open(CACHE_NAME)
            .then(function (cache) {
              return addAllNormalized(cache, batch, {
                bust: params.version,
                request: prefetchRequest,
                failAll: section === 'main',
              });
            })
            .then(function () {
              logGroup('Cached assets: ' + section, batch);
            })
            ['catch'](function (e) {
              console.error(e);
              throw e;
            });
        }

        function cacheChanged(section) {
          return getLastCache().then(function (args) {
            if (!args) {
              return cacheAssets(section);
            }

            var lastCache = args[0];
            var lastKeys = args[1];
            var lastData = args[2];

            var lastMap = lastData.hashmap;
            var lastVersion = lastData.version;

            if (!lastData.hashmap || lastVersion === params.version) {
              return cacheAssets(section);
            }

            var lastHashedAssets = Object.keys(lastMap).map(function (hash) {
              return lastMap[hash];
            });

            var lastUrls = lastKeys.map(function (req) {
              var url = new URL(req.url);
              url.search = '';
              url.hash = '';

              return url.toString();
            });

            var sectionAssets = assets[section];
            var moved = [];
            var changed = sectionAssets.filter(function (url) {
              if (lastUrls.indexOf(url) === -1 || lastHashedAssets.indexOf(url) === -1) {
                return true;
              }

              return false;
            });

            Object.keys(hashesMap).forEach(function (hash) {
              var asset = hashesMap[hash];

              // Return if not in sectionAssets or in changed or moved array
              if (sectionAssets.indexOf(asset) === -1 || changed.indexOf(asset) !== -1 || moved.indexOf(asset) !== -1) return;

              var lastAsset = lastMap[hash];

              if (lastAsset && lastUrls.indexOf(lastAsset) !== -1) {
                moved.push([lastAsset, asset]);
              } else {
                changed.push(asset);
              }
            });

            logGroup('Changed assets: ' + section, changed);
            logGroup('Moved assets: ' + section, moved);

            var movedResponses = Promise.all(
              moved.map(function (pair) {
                return lastCache.match(pair[0]).then(function (response) {
                  return [pair[1], response];
                });
              })
            );

            return caches.open(CACHE_NAME).then(function (cache) {
              var move = movedResponses.then(function (responses) {
                return Promise.all(
                  responses.map(function (pair) {
                    return cache.put(pair[0], pair[1]);
                  })
                );
              });

              return Promise.all([
                move,
                addAllNormalized(cache, changed, {
                  bust: params.version,
                  request: prefetchRequest,
                  failAll: section === 'main',
                  deleteFirst: section !== 'main',
                }),
              ]);
            });
          });
        }

        function deleteObsolete() {
          return caches.keys().then(function (keys) {
            var all = keys.map(function (key) {
              if (key.indexOf(CACHE_PREFIX) !== 0 || key.indexOf(CACHE_NAME) === 0) return;

              console.log('[SW]:', 'Delete cache:', key);
              return caches['delete'](key);
            });

            return Promise.all(all);
          });
        }

        function getLastCache() {
          return caches.keys().then(function (keys) {
            var index = keys.length;
            var key = undefined;

            while (index--) {
              key = keys[index];

              if (key.indexOf(CACHE_PREFIX) === 0) {
                break;
              }
            }

            if (!key) return;

            var cache = undefined;

            return caches
              .open(key)
              .then(function (_cache) {
                cache = _cache;
                return _cache.match(new URL(STORED_DATA_KEY, location).toString());
              })
              .then(function (response) {
                if (!response) return;

                return Promise.all([cache, cache.keys(), response.json()]);
              });
          });
        }

        function storeCacheData() {
          return caches.open(CACHE_NAME).then(function (cache) {
            var data = new Response(
              JSON.stringify({
                version: params.version,
                hashmap: hashesMap,
              })
            );

            return cache.put(new URL(STORED_DATA_KEY, location).toString(), data);
          });
        }

        self.addEventListener('fetch', function (event) {
          // Handle only GET requests
          if (event.request.method !== 'GET') {
            return;
          }

          // This prevents some weird issue with Chrome DevTools and 'only-if-cached'
          // Fixes issue #385, also ref to:
          // - https://github.com/paulirish/caltrainschedule.io/issues/49
          // - https://bugs.chromium.org/p/chromium/issues/detail?id=823392
          if (event.request.cache === 'only-if-cached' && event.request.mode !== 'same-origin') {
            return;
          }

          var url = new URL(event.request.url);
          url.hash = '';

          var urlString = url.toString();

          // Not external, so search part of the URL should be stripped,
          // if it's external URL, the search part should be kept
          if (externals.indexOf(urlString) === -1) {
            url.search = '';
            urlString = url.toString();
          }

          var assetMatches = allAssets.indexOf(urlString) !== -1;
          var cacheUrl = urlString;

          if (!assetMatches) {
            var cacheRewrite = matchCacheMap(event.request);

            if (cacheRewrite) {
              cacheUrl = cacheRewrite;
              assetMatches = true;
            }
          }

          if (!assetMatches) {
            // Use request.mode === 'navigate' instead of isNavigateRequest
            // because everything what supports navigationPreload supports
            // 'navigate' request.mode
            if (event.request.mode === 'navigate') {
              // Requesting with fetchWithPreload().
              // Preload is used only if navigationPreload is enabled and
              // navigationPreload mapping is not used.
              if (navigationPreload === true) {
                event.respondWith(fetchWithPreload(event));
                return;
              }
            }

            // Something else, positive, but not `true`
            if (navigationPreload) {
              var preloadedResponse = retrivePreloadedResponse(event);

              if (preloadedResponse) {
                event.respondWith(preloadedResponse);
                return;
              }
            }

            // Logic exists here if no cache match
            return;
          }

          // Cache handling/storing/fetching starts here
          var resource = undefined;

          if (responseStrategy === 'network-first') {
            resource = networkFirstResponse(event, urlString, cacheUrl);
          }
          // 'cache-first' otherwise
          // (responseStrategy has been validated before)
          else {
            resource = cacheFirstResponse(event, urlString, cacheUrl);
          }

          event.respondWith(resource);
        });

        self.addEventListener('message', function (e) {
          var data = e.data;
          if (!data) return;

          switch (data.action) {
            case 'skipWaiting':
              {
                if (self.skipWaiting) self.skipWaiting();
              }
              break;
          }
        });

        function cacheFirstResponse(event, urlString, cacheUrl) {
          handleNavigationPreload(event);

          return cachesMatch(cacheUrl, CACHE_NAME).then(function (response) {
            if (response) {
              if (DEBUG) {
                console.log('[SW]:', 'URL [' + cacheUrl + '](' + urlString + ') from cache');
              }

              return response;
            }

            // Load and cache known assets
            var fetching = fetch(event.request).then(function (response) {
              if (!response.ok) {
                if (DEBUG) {
                  console.log('[SW]:', 'URL [' + urlString + '] wrong response: [' + response.status + '] ' + response.type);
                }

                return response;
              }

              if (DEBUG) {
                console.log('[SW]:', 'URL [' + urlString + '] from network');
              }

              if (cacheUrl === urlString) {
                (function () {
                  var responseClone = response.clone();
                  var storing = caches
                    .open(CACHE_NAME)
                    .then(function (cache) {
                      return cache.put(urlString, responseClone);
                    })
                    .then(function () {
                      console.log('[SW]:', 'Cache asset: ' + urlString);
                    });

                  event.waitUntil(storing);
                })();
              }

              return response;
            });

            return fetching;
          });
        }

        function networkFirstResponse(event, urlString, cacheUrl) {
          return fetchWithPreload(event)
            .then(function (response) {
              if (response.ok) {
                if (DEBUG) {
                  console.log('[SW]:', 'URL [' + urlString + '] from network');
                }

                return response;
              }

              // Throw to reach the code in the catch below
              throw response;
            })
            [
              // This needs to be in a catch() and not just in the then() above
              // cause if your network is down, the fetch() will throw
              'catch'
            ](function (erroredResponse) {
              if (DEBUG) {
                console.log('[SW]:', 'URL [' + urlString + '] from cache if possible');
              }

              return cachesMatch(cacheUrl, CACHE_NAME).then(function (response) {
                if (response) {
                  return response;
                }

                if (erroredResponse instanceof Response) {
                  return erroredResponse;
                }

                // Not a response at this point, some other error
                throw erroredResponse;
                // return Response.error();
              });
            });
        }

        function handleNavigationPreload(event) {
          if (
            navigationPreload &&
            typeof navigationPreload.map === 'function' &&
            // Use request.mode === 'navigate' instead of isNavigateRequest
            // because everything what supports navigationPreload supports
            // 'navigate' request.mode
            event.preloadResponse &&
            event.request.mode === 'navigate'
          ) {
            var mapped = navigationPreload.map(new URL(event.request.url), event.request);

            if (mapped) {
              storePreloadedResponse(mapped, event);
            }
          }
        }

        // Temporary in-memory store for faster access
        var navigationPreloadStore = new Map();

        function storePreloadedResponse(_url, event) {
          var url = new URL(_url, location);
          var preloadResponsePromise = event.preloadResponse;

          navigationPreloadStore.set(preloadResponsePromise, {
            url: url,
            response: preloadResponsePromise,
          });

          var isSamePreload = function isSamePreload() {
            return navigationPreloadStore.has(preloadResponsePromise);
          };

          var storing = preloadResponsePromise.then(function (res) {
            // Return if preload isn't enabled or hasn't happened
            if (!res) return;

            // If navigationPreloadStore already consumed
            // or navigationPreloadStore already contains another preload,
            // then do not store anything and return
            if (!isSamePreload()) {
              return;
            }

            var clone = res.clone();

            // Storing the preload response for later consume (hasn't yet been consumed)
            return caches.open(PRELOAD_CACHE_NAME).then(function (cache) {
              if (!isSamePreload()) return;

              return cache.put(url, clone).then(function () {
                if (!isSamePreload()) {
                  return caches.open(PRELOAD_CACHE_NAME).then(function (cache) {
                    return cache['delete'](url);
                  });
                }
              });
            });
          });

          event.waitUntil(storing);
        }

        function retriveInMemoryPreloadedResponse(url) {
          if (!navigationPreloadStore) {
            return;
          }

          var foundResponse = undefined;
          var foundKey = undefined;

          navigationPreloadStore.forEach(function (store, key) {
            if (store.url.href === url.href) {
              foundResponse = store.response;
              foundKey = key;
            }
          });

          if (foundResponse) {
            navigationPreloadStore['delete'](foundKey);
            return foundResponse;
          }
        }

        function retrivePreloadedResponse(event) {
          var url = new URL(event.request.url);

          if (
            self.registration.navigationPreload &&
            navigationPreload &&
            navigationPreload.test &&
            navigationPreload.test(url, event.request)
          ) {
          } else {
            return;
          }

          var fromMemory = retriveInMemoryPreloadedResponse(url);
          var request = event.request;

          if (fromMemory) {
            event.waitUntil(
              caches.open(PRELOAD_CACHE_NAME).then(function (cache) {
                return cache['delete'](request);
              })
            );

            return fromMemory;
          }

          return cachesMatch(request, PRELOAD_CACHE_NAME).then(function (response) {
            if (response) {
              event.waitUntil(
                caches.open(PRELOAD_CACHE_NAME).then(function (cache) {
                  return cache['delete'](request);
                })
              );
            }

            return response || fetch(event.request);
          });
        }

        function mapAssets() {
          Object.keys(assets).forEach(function (key) {
            assets[key] = assets[key].map(function (path) {
              var url = new URL(path, location);

              url.hash = '';

              if (externals.indexOf(path) === -1) {
                url.search = '';
              }

              return url.toString();
            });
          });

          hashesMap = Object.keys(hashesMap).reduce(function (result, hash) {
            var url = new URL(hashesMap[hash], location);
            url.search = '';
            url.hash = '';

            result[hash] = url.toString();
            return result;
          }, {});

          externals = externals.map(function (path) {
            var url = new URL(path, location);
            url.hash = '';

            return url.toString();
          });
        }

        function addAllNormalized(cache, requests, options) {
          var bustValue = options.bust;
          var failAll = options.failAll !== false;
          var deleteFirst = options.deleteFirst === true;
          var requestInit = options.request || {
            credentials: 'omit',
            mode: 'cors',
          };

          var deleting = Promise.resolve();

          if (deleteFirst) {
            deleting = Promise.all(
              requests.map(function (request) {
                return cache['delete'](request)['catch'](function () {});
              })
            );
          }

          return Promise.all(
            requests.map(function (request) {
              if (bustValue) {
                request = applyCacheBust(request, bustValue);
              }

              return fetch(request, requestInit)
                .then(fixRedirectedResponse)
                .then(
                  function (response) {
                    if (!response.ok) {
                      return { error: true };
                    }

                    return { response: response };
                  },
                  function () {
                    return { error: true };
                  }
                );
            })
          ).then(function (responses) {
            if (
              failAll &&
              responses.some(function (data) {
                return data.error;
              })
            ) {
              return Promise.reject(new Error('Wrong response status'));
            }

            if (!failAll) {
              responses = responses.filter(function (data) {
                return !data.error;
              });
            }

            return deleting.then(function () {
              var addAll = responses.map(function (_ref, i) {
                var response = _ref.response;

                return cache.put(requests[i], response);
              });

              return Promise.all(addAll);
            });
          });
        }

        function matchCacheMap(request) {
          var urlString = request.url;
          var url = new URL(urlString);

          var requestType = undefined;

          if (isNavigateRequest(request)) {
            requestType = 'navigate';
          } else if (url.origin === location.origin) {
            requestType = 'same-origin';
          } else {
            requestType = 'cross-origin';
          }

          for (var i = 0; i < cacheMaps.length; i++) {
            var map = cacheMaps[i];

            if (!map) continue;
            if (map.requestTypes && map.requestTypes.indexOf(requestType) === -1) {
              continue;
            }

            var newString = undefined;

            if (typeof map.match === 'function') {
              newString = map.match(url, request);
            } else {
              newString = urlString.replace(map.match, map.to);
            }

            if (newString && newString !== urlString) {
              return newString;
            }
          }
        }

        function fetchWithPreload(event) {
          if (!event.preloadResponse || navigationPreload !== true) {
            return fetch(event.request);
          }

          return event.preloadResponse.then(function (response) {
            return response || fetch(event.request);
          });
        }
      }

      function cachesMatch(request, cacheName) {
        return caches
          .match(request, {
            cacheName: cacheName,
          })
          .then(function (response) {
            if (isNotRedirectedResponse(response)) {
              return response;
            }

            // Fix already cached redirected responses
            return fixRedirectedResponse(response).then(function (fixedResponse) {
              return caches
                .open(cacheName)
                .then(function (cache) {
                  return cache.put(request, fixedResponse);
                })
                .then(function () {
                  return fixedResponse;
                });
            });
          })
          [
            // Return void if error happened (cache not found)
            'catch'
          ](function () {});
      }

      function applyCacheBust(asset, key) {
        var hasQuery = asset.indexOf('?') !== -1;
        return asset + (hasQuery ? '&' : '?') + '__uncache=' + encodeURIComponent(key);
      }

      function isNavigateRequest(request) {
        return (
          request.mode === 'navigate' ||
          request.headers.get('Upgrade-Insecure-Requests') ||
          (request.headers.get('Accept') || '').indexOf('text/html') !== -1
        );
      }

      function isNotRedirectedResponse(response) {
        return !response || !response.redirected || !response.ok || response.type === 'opaqueredirect';
      }

      // Based on https://github.com/GoogleChrome/sw-precache/pull/241/files#diff-3ee9060dc7a312c6a822cac63a8c630bR85
      function fixRedirectedResponse(response) {
        if (isNotRedirectedResponse(response)) {
          return Promise.resolve(response);
        }

        var body = 'body' in response ? Promise.resolve(response.body) : response.blob();

        return body.then(function (data) {
          return new Response(data, {
            headers: response.headers,
            status: response.status,
          });
        });
      }

      function copyObject(original) {
        return Object.keys(original).reduce(function (result, key) {
          result[key] = original[key];
          return result;
        }, {});
      }

      function logGroup(title, assets) {
        console.groupCollapsed('[SW]:', title);

        assets.forEach(function (asset) {
          console.log('Asset:', asset);
        });

        console.groupEnd();
      }
      WebpackServiceWorker(__wpo, {
        loaders: {},
        cacheMaps: [
          {
            match: function (url) {
              if (url.pathname === location.pathname) {
                return;
              }

              return new URL('/', location);
            },
            to: null,
            requestTypes: ['navigate'],
          },
        ],
        navigationPreload: false,
      });
      module.exports = __webpack_require__('6872a71ed75a597694c7');

      /***/
    },

    /***/ '6872a71ed75a597694c7': /***/ function (module, exports) {
      /***/
    },

    /******/
  }
);
